version: "3.9"

services:
  localstack:
    container_name: localstack
    image: localstack/localstack:latest
    ports:
      - "4566:4566"   # Endpoint principal AWS
      - "8080:8080"   # UI intégrée
    env_file:
      - ./localstack.env
    environment:
      SERVICES: "s3,dynamodb,lambda,apigateway,sqs,sts,iam"
      LAMBDA_EXECUTOR: "docker-reuse"
      HOSTNAME_EXTERNAL: "localhost"
      AWS_DEFAULT_REGION: "us-east-1"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./localstack-data:/var/lib/localstack/data"
      - "../backend:/opt/code"  # Permet d'exécuter les lambdas depuis ton projet
